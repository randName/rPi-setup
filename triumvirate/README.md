# Text Triumvirate
---

```shell
# Install
apt-get install zsh vim tmux
apt-get remove vim-tiny

# install oh-my-zsh
sh -c "$(wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)"

# get dotfiles
git clone https://github.com/randname/dotfiles.git
```
